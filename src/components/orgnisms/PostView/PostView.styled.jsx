import styled from 'styled-components'

const StyledPostView = styled.div`
    p {
        font-size: 1.6rem;
        font-weight: bold;
        padding: 1rem;
        text-align: center;
        span {
            padding: 0 2.2rem;
        }
    }
    .posts-ttl {
        font-size: 1.8rem;
        font-weight: bold;
        padding: 0 1rem
    }

`

export default StyledPostView;

