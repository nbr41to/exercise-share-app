import styled from 'styled-components'

const StyledComponent = styled.div`
    .top-msg {
        font-size:2.0rem;
        font-weight: bold;
        text-align: center;
        padding: 1rem
    }
    .user-info {
        font-size: 1.4rem;
        text-align: right;
        padding: 1rem
    }
`

export default StyledComponent; 
